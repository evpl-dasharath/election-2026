--- frontend/src/pages/PartyPage.tsx
+++ frontend/src/pages/PartyPage.tsx
@@ -4,7 +4,7 @@
 import GlobalHeader from '../components/GlobalHeader';
 import { resolveCardBg, ALLIANCE_COLORS, isRawIND, resolvePartyColor } from '../utils/colorUtils';
 import { partyAbbr, partyDisplay } from '../utils/partyAbbr';
-import type { PartyDetail, ConstituencyListItem, Region, SeatClass } from '../types';
+import type { PartyDetail, ConstituencyListItem, Region } from '../types';
 import {
   LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid, Legend
 } from 'recharts';
@@ -55,7 +55,6 @@
         c.status === 'IN_PROGRESS' || c.status === 'RESULT_DECLARED';

       if (hasCountingStarted && c.leader) {
-        if (c.placing && c.placing[0] && partyAbbr(c.placing[0].party) === pa) wonLead++;
         if (partyAbbr(c.leader.party) === pa) wonLead++; // Fallback to leader.party
       }
     });
@@ -360,7 +359,7 @@
                       // Format votes as 'XX,XXX'
                       const numStr = value.toLocaleString('en-IN');
                       const m1 = numStr.match(/^(\d{1,3}(,\d{2})*)(,\d{3})$/);
-                      const m2 = numStr.match(/^(\d{1,3})$/);
+                      const m2 = numStr.match(/^(\d{1,3})$/) || ["", numStr];

                       return (
                         <div style={{ background: '#fff', border: '1px solid #E2DDD8', padding: '8px 12px', borderRadius: 8, boxShadow: '0 4px 12px rgba(0,0,0,0.08)' }}>
