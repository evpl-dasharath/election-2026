## Environment

- OS: Windows
- Shell: PowerShell
- NEVER use `&&` to chain commands. PowerShell does not support it.
- Always run commands on separate lines, or use `;` as a separator.
- Use `python` not `python3`.